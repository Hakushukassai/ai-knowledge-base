# 外部Skill 安全導入ガイド

外部(GitHubの第三者リポジトリや、Anthropic公式のものを含む)のClaude Code Skillをこの知識ベースに取り込む時の手順。2026-08-24に実際に7件導入・3件見送りした経験をもとにまとめた。

候補→確定ルールという通常の品質ゲート(2プロジェクトでの確認)を意図的に迂回する行為なので、代わりにこの手順で安全性と追跡可能性を担保する。

## 0. 前提: 一度に1件だけ

複数候補があっても、最初の1件が正しくダッシュボードに反映され、実際に発動することを確認してから次に進む。一括導入はしない。

## 1. スコープ判定: そもそも「単純なSkill」か?

取得元を覗く前に、READMEやディレクトリ構成から次を確認する。該当したら**この手順の対象外**、フェーズ2以降に進まず、ユーザーに判断を仰ぐ:

- `pip install`や`npm install`などパッケージインストールが前提
- カメラ・画面収録・マイクなどOSレベルの権限が必要
- 独自の`hooks.json`など、Claude Code自身の自動化を追加・変更するものを同梱
- `.claude-plugin/`ディレクトリがある(＝「Skill」ではなく「プラグイン」)
- SKILL.mdが10件以上ある、専用MCPサーバーを同梱している等、単体Skillではなく統合フレームワーク規模

(実例: `claude-vision`はWebカメラ権限+pip install+独自hooksで対象外、`godot-ai-builder`はSKILL.md16件+MCPサーバーで対象外、`gamedev-claude-plugins`はプラグイン形式で対象外。)

単純な「SKILL.md(+参照用mdファイル、あればスクリプト数本)」の構成であれば次に進む。

## 2. 取得(必ずユーザー自身が実行)

外部リポジトリの`git clone`はAIが自ら実行しない。ユーザーに`git clone`コマンドを提示し、実行してもらう。

- **注意**: `~/Downloads`は、セッション再開後にmacOSのプライバシー保護で読み取り不能になることがある(`Operation not permitted`)。その場合はプロジェクト配下など別の場所への再取得を依頼する。

## 3. 取得内容のレビュー(ここはAIが読み取り専用で実施)

1. **フォーマット確認**: `SKILL.md`にYAMLフロントマター(`name`/`description`)があるか。無ければ追加する。`description`は「いつ発動すべきか」を具体的かつ強めに書く([SKILL-PROMOTION-GUIDE.md](SKILL-PROMOTION-GUIDE.md)と同じ基準)。
2. **機密情報スキャン**: `sh ~/knowledge-base/scripts/pyrun.sh ~/knowledge-base/claude-config/hooks/scan_secrets.py <取得先フォルダ>` を実行する。
   - ドキュメント中のプレースホルダーメールアドレスが誤検知されることが多い。`@`を含まない表記(`user(at)example.com`のように)に書き換えて解消する。意味は変えない。
   - **AI自身が誤検知の説明文を書く時も、`@`付きの実例をそのまま書かない**こと(このガイド自身の初稿・候補ファイルの本文が、まさにその書き方で誤検知した実例あり)。
3. **同梱スクリプトのレビュー**: `.py`等の実行可能ファイルがあれば、コピーする前に全文読む。確認する観点: ネットワーク通信の有無、`eval`/`exec`の有無、難読化されていないか、認証情報を収集していないか。問題なければそのまま活かす(書き換えない)。
4. **既存Skillとの重複・矛盾確認**: 導入済みSkillと話題が被らないか、矛盾する助言をしないか目視する。多少の補完関係(例: ゲーム実装Skillが最適化Skillの閾値に触れていない)は許容範囲、記録だけ残す。
5. **ライセンス確認**: MIT/Apache等の寛容なライセンスなら次に進む。「All rights reserved」等の場合、本来の配布経路(`/plugin install`等)が使えるか確認し、使えるならそちらを案内する。使えない場合のみ、個人利用目的での直接配置を検討し、フロントマターに`license_note`として経緯を明記する。

## 4. 配置

- 置き場所: `~/.claude/skills/<スキル名>/`(全プロジェクト共通)。特定プロジェクト専用なら`<プロジェクト>/.claude/skills/<スキル名>/`。
- SKILL.mdが`references/`配下のファイルを参照する指示を含む場合、そのファイルも一緒に配置する(無いと「読めと言われたファイルが存在しない」壊れた状態になる)。
- `name`が汎用的すぎる場合(例: 単に`godot`)は、既存の命名慣習に合わせてリネームする(例: `godot-testing-deploy`)。リネームした事実は記録に残す。
- 配置後、**コピー先に対して**もう一度`scan_secrets.py`を実行し、クリーンであることを最終確認する。

## 5. 出所の記録

`external-skill-imports/`(`candidates/`とは別の場所。純粋な技術的発見の昇格キューと混ざらないよう分離している)に、既存のcandidateスキーマ + 以下2フィールドで記録する:

```
source: external
source_url: <取得元URL>
```

本文には「通常の2プロジェクト確認ゲートを迂回して直接Skill化している」旨を明記する。これによりダッシュボードの「外部Skill導入記録」セクションに「外部由来・未検証」バッジ付きで自動表示される。

## 6. 反映確認

1. `bash scripts/generate_stats.sh` を実行し、ダッシュボードの「Skill一覧」件数が増え、内容が正しく表示されるか確認する。
2. `file://`での直接閲覧はダッシュボードが大きくなるとページサイズの都合で不安定になることがある。確認は`python3 -m http.server`等の簡易サーバー経由が安定する(確認後は停止する)。
3. 可能なら実際にSkillツール経由で一度呼び出し、自然な発動と回答の妥当性を確認する(設置できていることの確認と、実際に役立つ内容かの確認は別物)。

## 7. 同期・push

AIに「KBをGitHubに同期して」と依頼し、`kb-sync`の検証・機密情報スキャン・commit・pushを実行する。1件ずつ個別にpushする(まとめてpushすると、後から特定の1件だけ問題が見つかった時に切り分けにくい)。

## 8. 通常フローへの合流

実際に使ってみて役立った・ハマったという気づきが出たら、外部由来だからと特別扱いせず、通常の候補記録・人による承認・複数プロジェクトでの再確認と同じ昇格フローに乗せる。

---

## 導入実績(2026-08-24時点)

| Skill | 判定 | 備考 |
|---|---|---|
| `godot-code-gen` | 導入・発動確認済み | フロントマター無しだったため追加 |
| `game-feel` | 導入 | フロントマター完備 |
| `godot-ui-control` | 導入 | フロントマター完備 |
| `godot-animation` | 導入 | フロントマター完備 |
| `godot-testing-deploy` | 導入 | 元名`godot`からリネーム、同梱スクリプト4本をレビュー |
| `ux-designer` | 導入・発動確認済み | プレースホルダーメール14箇所を修正 |
| `frontend-design` | 導入 | Anthropic公式、ライセンス差異あり(直接配置は代替手段) |
| `claude-vision` | 見送り | プラグイン形式、Webカメラ権限、pip install必要 |
| `godot-ai-builder` | 見送り | SKILL.md16件+MCPサーバー、フレームワーク規模 |
| `gamedev-claude-plugins` | 見送り | プラグインマーケットプレイス形式 |
| `godot-audio` | 導入・発動確認済み | DEPTH//の音声バス切り替えバグに関連。フロントマター完備 |
| `godot-physics` | 導入・発動確認済み | RESODIVE/DEPTH//の衝突判定に関連。フロントマター完備 |
| `godot-signals-groups` | 導入・発動確認済み | フロントマター完備 |
| `godot-3d-essentials` | 導入・発動確認済み | godot-optimizationが3D前提と判明したため導入。フロントマター完備 |
| `game-ai` | 導入・発動確認済み | `skills/disciplines/`配下のエンジン非依存Skill。参照ファイル2件、フロントマター完備 |
